 radius：freeradius-3.0.4-6.el7.x86_64、freeradius-ldap-3.0.4-6.el7.x86_64、freeradius-utils-3.0.4-6.el7.x86_64(后两个是依赖包)
 deploy step:
 etc/raddb/mods-available/ldap
 1)ldap {
  server = "ldap server"
  port = 389 /default if use ssl ,please change the port number
  identity = "admin"
  password = "admin -password"
  base_dn = "search branch" #若想控制认证的范围，可以修改这行的搜索范围
}
user {
    base_dn = "${..base_dn}"
    filter = "(employeeID[<---just example]=%{%{Stripped-User-Name}:-%{User-Name}})"
    scope = 'sub' //search scope
}
etc/raddb/sites-available/default
2)default
 authorize{  // open the module ldap
 ldap
  if ((ok ||updated) && User-Password){
            update{
                  control:Auth-Type :=ldap
      }
  }
}
authenticate{
    Auth-Type LDAP {
    ldap
  }
} 
etc/raddb/clients.conf
3)clients.conf
 client ip/hostname { // add the server you need to listen
    secret = 'you set'
  }
  
4) activiate the ldap module
  $cd raddb/mods-enabled
  $ln -s ../mods-available/ldap //if you want to activiate other module ,change the module name here
  
5) debug
radtest testname testpassword localhost 0 secret  //test radius server whether work normal

  
